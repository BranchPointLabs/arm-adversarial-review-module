import React from "react";

export default function ReviewFeedScreen() {
  return (
    <div className="page">
      <div className="surfaceCard">
        <div className="surfaceTitle">Review Feed</div>
        <div className="surfaceCopy">
          The original plan is card-first: agent runs should emit a few concise cards, not essays. The schema is
          present, but the feed and accept or reject loop are not yet implemented.
        </div>
      </div>
    </div>
  );
}
